server:
  port: 8024

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: spring-elastic-search-gradle

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true